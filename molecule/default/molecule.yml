---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8

platforms:
  # Debian 9 Stretch
  - name: debian9
    image: debian:9
